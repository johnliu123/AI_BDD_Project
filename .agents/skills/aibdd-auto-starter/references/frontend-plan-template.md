# Frontend Plan Template

> **Variant**：AIBDD frontend（Next.js + Storybook + playwright-bdd）。三層 layered plan 的精簡版。
> **Scope**：§1 API & Mock · §2 Component · §3 Page。
> **Deferred until ≥ 2nd spec package or sustained scaling pain**：§4 Dependency Graph、§5 Pre-Red Scan Checklist。
> **Generated by**：`/aibdd-plan`（frontend mode）
> **Spec package**：`${TRUTH_FUNCTION_PACKAGE}`
>
> 本檔示範一個假想 spec package「`05-借閱申請`」的最終長相；落到實際 package 時把 placeholder 換成真值即可。

---

## Inputs（Plan 的所有 truth 來源）

| 來源 | 路徑 | 角色 |
|---|---|---|
| OpenAPI contract | `${API_CONTRACT_DIR}/borrow-request.yml` | §1 truth：typed API client + 自寫 mock impl |
| 後端 features（上游） | `<backend-spec-root>/<boundary>/packages/05-借閱申請/features/*.feature` | 行為素材；UI scenario 由此衍生（投影 / 對映） |
| 前端 features（UI vocab） | `${FEATURE_SPECS_DIR}/*.feature` | §3 truth：Page acceptance |
| Component library 候選 | 經 `${PROJECT_SLUG}-sb-mcp` MCP 工具查詢 | §2 候選池（list-all-documentation → get-documentation） |
| Dev constitution | `.aibdd__dev-constitution.md` | 分層擁有 / 依賴方向 |
| BDD axes | `.aibdd__bdd-stack__project-bdd-axes.md` | filename / archive / pre-hook 軸 |

---

## §1 API & Mock Plan

> Truth：OpenAPI。一條 `operationId` 一列。**禁止** MSW；自寫 mock 函式 + fixture 是本 package 唯一 mock 路徑。

### §1.1 Typed API Client Functions

| operationId | Method × Path | Module | Request Schema | Response Schema | Notes |
|---|---|---|---|---|---|
| `createBorrowRequest` | `POST /borrow-requests` | `src/apis/borrow-request-api.ts` | `BorrowRequestPayloadSchema` | `BorrowRequestSchema` | header 帶 `Idempotency-Key` |
| `getBorrowRequest` | `GET /borrow-requests/{id}` | 同上 | – | `BorrowRequestSchema` | 404 走 `not-found.tsx` |
| `listBorrowRequests` | `GET /borrow-requests` | 同上 | `ListQuerySchema` | `Page<BorrowRequestSchema>` | server-side cursor |

### §1.2 Zod Schemas

| Schema | File | OpenAPI `$ref` |
|---|---|---|
| `BorrowRequestSchema` | `src/lib/schemas/borrow-request.ts` | `#/components/schemas/BorrowRequest` |
| `BorrowRequestPayloadSchema` | 同上 | `#/components/schemas/BorrowRequestPayload` |
| `ListQuerySchema` | `src/lib/schemas/list-query.ts` | shared |

> `z.infer<typeof XSchema>` 為 §2/§3 的 props / state 型別 SSOT；**禁止**手寫對應 `interface`。

### §1.3 Mock Implementations（custom — NO MSW）

> 每個 mock 是純 typed function，回傳 fixture-shaped data；同時供 §2 Storybook stories 與 §3 e2e fixtures 消費。**禁止**任何 `setupWorker` / `mockServiceWorker.js` 引入。

| Mock fn | File | Backs operationId | Fault scenarios |
|---|---|---|---|
| `mockCreateBorrowRequest` | `src/mocks/borrow-request.ts` | `createBorrowRequest` | 200 success / 400 validation / 409 duplicate-id |
| `mockGetBorrowRequest` | 同上 | `getBorrowRequest` | 200 / 404 |
| `mockListBorrowRequests` | 同上 | `listBorrowRequests` | empty / 1 page / multi-page |

| Fixture builder | File |
|---|---|
| `borrowRequestFixture(overrides?)` | `src/mocks/fixtures/borrow-request.ts` |

### §1.4 Wire-up

- Test / dev mode：API client 透過 `src/lib/api-client.ts::resolveTransport()` 讀 `NEXT_PUBLIC_API_TRANSPORT=mock`，在 process 內把 `operationId → mock fn` 直接路由。
- Production build：以 build-time guard（`process.env.NODE_ENV === 'production'` 檢查）拒絕 `src/mocks/**` 被 import；漏網 → bundle analyzer 在 CI 攔截（規則化在 §4 補）。

---

## §2 Component Plan

> Truth：Storybook（每個元件的 `*.stories.ts` 是合約）。`Library candidate` 欄位由 MCP 查詢結果填入；**未經 MCP 驗證的 props 不准進入此表**。

### §2.1 Component Inventory

| Component | File | Library candidate (MCP-verified) | Props（intent） | a11y role |
|---|---|---|---|---|
| `BorrowRequestForm` | `src/components/borrow-request/BorrowRequestForm.tsx` | TBD via MCP（先列候選後查 doc） | `onSubmit`, `defaultValues?`, `disabled?` | `form` |
| `BorrowRequestList` | `src/components/borrow-request/BorrowRequestList.tsx` | TBD via MCP | `items`, `onSelect` | `list` |
| `BorrowRequestStatusBadge` | `src/components/borrow-request/StatusBadge.tsx` | TBD via MCP | `status` | `status` |

> **MCP usage protocol（呼應 AGENTS.md）**：填 props 之前先呼 `list-all-documentation` → `get-documentation` 確認候選元件已支援該 prop；屬性未在文件中出現 → 不得依命名直覺臆測，需要寫自訂元件並在本表標 `custom: yes`。

### §2.2 Storybook Story Matrix

| Component | States to cover（一 story 一 state） |
|---|---|
| `BorrowRequestForm` | Default / Filled / Submitting / ServerError(400) / ServerError(409) |
| `BorrowRequestList` | Empty / Single / Page-of-10 / Page-loading |
| `BorrowRequestStatusBadge` | pending / approved / rejected / cancelled |

> 每個 story 從 `src/mocks/fixtures/*`（§1.3）拿資料；**禁止**在 story 內手寫 inline literal（會跟 schema 漂移無感）。

### §2.3 Quality bars（C-lite 必過）

- `addon-a11y` 對每個 story 通過（no critical violations）。
- `addon-vitest` interaction test 至少覆蓋：表單 happy path × 1 + 失敗路徑 × 1。
- 視覺回歸（Chromatic / test-runner）deferred 至 §4 — 不在 C-lite 範圍。

---

## §3 Page Plan

> Truth：`${FEATURE_SPECS_DIR}/*.feature`。一條 route 一列；scenario reference 為合法紅燈的對映依據。

### §3.1 Route Map

| URL | File | Server / Client | Composes (§2) | Calls (§1) | Backing scenarios |
|---|---|---|---|---|---|
| `/borrow-requests/new` | `src/app/borrow-requests/new/page.tsx` | client（表單） | `BorrowRequestForm` | `createBorrowRequest` | `01-提交借閱申請.feature::S1/S2` |
| `/borrow-requests` | `src/app/borrow-requests/page.tsx` | server + client subtree | `BorrowRequestList`, `StatusBadge` | `listBorrowRequests` | `02-列出借閱申請.feature::S1` |
| `/borrow-requests/[id]` | `src/app/borrow-requests/[id]/page.tsx` | server | `StatusBadge`（+ 詳情區） | `getBorrowRequest` | `03-檢視借閱申請.feature::S1/S2` |

### §3.2 Server / Client boundary policy

- 預設 **server component**；只有需要互動 state 的 subtree 才 `'use client'`。
- API client **僅**在 server component 直接呼叫；client component 透過 props / route segment fetch 取得資料。
- `env.ts`（`src/lib/env.ts`）只在 module top-level 消費；**禁止**在 render body 內讀 `process.env`。

### §3.3 Route-level UX hooks

| Concern | Handling |
|---|---|
| Loading | `loading.tsx` per segment |
| Error | `error.tsx` per segment，使用 `getErrorMessage(error)` 安全 narrow |
| Not found | `not-found.tsx` for `/borrow-requests/[id]` |
| Redirect after submit | client component → `useRouter().push('/borrow-requests')` |

---

## Open Questions（plan 階段需 reconcile，不直接做決定）

1. `BorrowRequestForm` 是否採 React Hook Form？影響 §2.1 props 型別與 a11y label 寫法。
2. List 分頁是 cursor 還是 offset？影響 `ListQuerySchema` 與 §1.3 mock 多頁邏輯。
3. 是否需要新增 `_ui_only/` 子目錄涵蓋 client-side validation 即時提示之 scenario？

---

## Deferred（out of C-lite scope）

| 段 | 補上的觸發條件 |
|---|---|
| §4 Dependency Graph | 第 2 個 spec package 進場 / 本 package task 數 ≥ 12 / 共用元件跨 package 出現 |
| §5 Pre-Red Scan Checklist | `prehandling-before-red-phase.md` 落版 / OpenAPI 變動已造成過漏掃 mock 一次以上 |

> 一旦補上 §4/§5 → 從 **C-lite** 升 **C-full**；本檔 header 同步把 “C-lite” 改成 “C-full”，並把 Deferred 表清空。
